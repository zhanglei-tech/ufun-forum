<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.ufun.forum.mapper.ForumPostMapper">

    <resultMap type="tech.ufun.forum.domain.ForumPost" id="ForumPostMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="postId" column="post_id" jdbcType="VARCHAR"/>
        <result property="topicId" column="topic_id" jdbcType="VARCHAR"/>
        <result property="topic" column="topic" jdbcType="VARCHAR"/>
        <result property="author" column="author" jdbcType="VARCHAR"/>
        <result property="authorAvatar" column="author_avatar" jdbcType="VARCHAR"/>
        <result property="authorId" column="author_id" jdbcType="VARCHAR"/>
        <result property="subject" column="subject" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="isTop" column="is_top" jdbcType="INTEGER"/>
        <result property="viewCount" column="view_count" jdbcType="INTEGER"/>
        <result property="replyCount" column="reply_count" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="findById" resultMap="ForumPostMap">
        select
          id, post_id, topic_id, topic, author, author_avatar, author_id, subject, content, create_time, is_top, view_count, reply_count
        from forum_post
        where post_id = #{postId}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="findAll" resultMap="ForumPostMap">
        select
          id, post_id, topic_id, topic, author, author_avatar, author_id, subject, create_time, is_top, view_count, reply_count
        from forum_post
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="postId != null and postId != ''">
                and post_id = #{postId}
            </if>
            <if test="topicId != null and topicId != ''">
                and topic_id = #{topicId}
            </if>
            <if test="topic != null and topic != ''">
                and topic = #{topic}
            </if>
            <if test="author != null and author != ''">
                and author = #{author}
            </if>
            <if test="authorAvatar != null and authorAvatar != ''">
                and author_avatar = #{authorAvatar}
            </if>
            <if test="authorId != null and authorId != ''">
                and author_id = #{authorId}
            </if>
            <if test="subject != null and subject != ''">
                and subject = #{subject}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="isTop != null">
                and is_top = #{isTop}
            </if>
            <if test="viewCount != null">
                and view_count = #{viewCount}
            </if>
            <if test="replyCount != null">
                and reply_count = #{replyCount}
            </if>
        </where>
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="findAllTop" resultMap="ForumPostMap">
        select
          id, post_id, topic_id, topic, author, author_avatar, author_id, subject, create_time, view_count, reply_count
        from forum_post
        where is_top = 1
        order by create_time desc
    </select>

    <!--新增所有列-->
    <insert id="insert">
        insert into forum_post(post_id, topic_id, topic, author, author_avatar, author_id, subject, content, create_time, is_top, view_count, reply_count)
        values (#{postId}, #{topicId}, #{topic}, #{author}, #{authorAvatar}, #{authorId}, #{subject}, #{content}, #{createTime}, #{isTop}, #{viewCount}, #{replyCount})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update forum_post
        <set>
            <if test="topicId != null and topicId != ''">
                topic_id = #{topicId},
            </if>
            <if test="topic != null and topic != ''">
                topic = #{topic},
            </if>
            <if test="author != null and author != ''">
                author = #{author},
            </if>
            <if test="authorAvatar != null and authorAvatar != ''">
                author_avatar = #{authorAvatar},
            </if>
            <if test="authorId != null and authorId != ''">
                author_id = #{authorId},
            </if>
            <if test="subject != null and subject != ''">
                subject = #{subject},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="isTop != null">
                is_top = #{isTop},
            </if>
            <if test="viewCount != null">
                view_count = #{viewCount},
            </if>
            <if test="replyCount != null">
                reply_count = #{replyCount},
            </if>
        </set>
        where post_id = #{postId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from forum_post where post_id = #{postId}
    </delete>

</mapper>