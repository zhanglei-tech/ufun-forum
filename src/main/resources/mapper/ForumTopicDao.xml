<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.ufun.forum.mapper.ForumTopicMapper">

    <resultMap type="tech.ufun.forum.domain.ForumTopic" id="ForumTopicMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="topicId" column="topic_id" jdbcType="VARCHAR"/>
        <result property="topic" column="topic" jdbcType="VARCHAR"/>
        <result property="topicKey" column="topic_key" jdbcType="VARCHAR"/>
        <result property="listOrder" column="list_order" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="findById" resultMap="ForumTopicMap">
        select
          id, topic_id, topic, topic_key, list_order, status
        from forum_topic
        where id = #{id}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="findAll" resultMap="ForumTopicMap">
        select
          id, topic_id, topic, topic_key, list_order, status
        from forum_topic where status = 1
    </select>

    <!--新增所有列-->
    <insert id="insert">
        insert into forum_topic(topic_id, topic, topic_key, list_order, status)
        values (#{topicId}, #{topic}, #{topicKey}, #{listOrder}, #{status})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update forum_topic
        <set>
            <if test="topicId != null and topicId != ''">
                topic_id = #{topicId},
            </if>
            <if test="topic != null and topic != ''">
                topic = #{topic},
            </if>
            <if test="topicKey != null and topicKey != ''">
                topic_key = #{topicKey},
            </if>
            <if test="listOrder != null">
                list_order = #{listOrder},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from forum_topic where id = #{id}
    </delete>

</mapper>